version: 2
updates:
- package-ecosystem: "maven"
  directory: "/"
  schedule:
    interval: "daily"
  # Enable version updates along with security updates, limit to 10 to prevent CI overload
  open-pull-requests-limit: 10
